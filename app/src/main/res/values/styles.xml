<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SignIn" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogTitleText">Sign In Account</item>
        <item name="DialogSpotColor">@color/white</item>
        <item name="DialogSpotCount">5</item>
    </style>

    <style name="SignUp" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogTitleText">Sign Up Account</item>
        <item name="DialogSpotColor">@color/white</item>
        <item name="DialogSpotCount">5</item>
    </style>

    <style name="Loading" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogTitleText">Loading</item>
        <item name="DialogSpotColor">@color/white</item>
        <item name="DialogSpotCount">5</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="MonstserratMedium" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/mm</item>
        <item name="android:textColor">@color/textColorAccent</item>
        <!--        <item name="android:font">@font/mm</item>-->
    </style>


    <!--    <style name="TextToolbarTheme" parent="ThemeOverlay.AppCompat.Light">-->
    <!--        <item name="android:fontFamily">@font/mm</item>-->
    <!--        <item name="android:textColor">@color/cancelled</item>-->
    <!--    </style>-->

    <!-- Dialog -->
    <style name="RatingDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:background">@color/white</item>
        <item name="buttonBarPositiveButtonStyle">@style/RatingDialogButtonStyle</item>
        <item name="buttonBarNegativeButtonStyle">@style/RatingDialogButtonStyle</item>
        <item name="buttonBarNeutralButtonStyle">@style/RatingDialogButtonStyle</item>
    </style>

    <style name="RatingDialogButtonStyle" parent="@style/Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:textSize">@dimen/text_size_medium</item>
    </style>

    <style name="RatingDialogFadeAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <style name="MyToolbarThemeSimple.Base" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:theme">@style/ToolbarTheme</item>
    </style>

    <style name="ToolbarTheme">

        <item name="android:fontFamily">@font/mm</item>
        <item name="android:textColor">@color/white</item>

        <!-- Used to for the title of the toolbar -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <!-- Used to color back button and overflow menu dots -->
        <item name="android:textColorSecondary">@android:color/white</item>

        <item name="android:background">@color/colorPrimary</item>
    </style>

</resources>